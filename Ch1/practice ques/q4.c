# include<stdio.h>

int main() {
    int number;
    printf("Enter the desired number for cube: ");
    scanf("%d",&number);
    printf("The Cube of Number is : %d", number * number * number);
    return 0;
}